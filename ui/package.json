{
  "name": "poke-radar-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "tauri:dev": "npm run build && cd .. && cargo tauri dev",
    "tauri:dev:npx": "npm run build && npx --no-install tauri dev --config ../src-tauri/tauri.conf.json"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.2",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.2.0",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "jsdom": "^26.0.0",
    "typescript": "^5.7.3",
    "vite": "^7.1.11",
    "vitest": "^3.0.5"
  }
}
